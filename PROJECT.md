# PROJECT.md — A 股 AI 量化分析系统 (SSOT 架构极密总纲)

> **⚠ 严正指令 (SYSTEM DIRECTIVE AND PROTOCOLS)**
> 本文件是本大型重度数据投研分析项目**唯一且绝对核心的 Single Source of Truth (SSOT)**。无论您是属于哪个版本的底层语言驱动核心（或新接引加入系统的 AI 逻辑演化助手），在开始进行所有崭新的架构构思讨论、漏洞填补、和任何系统故障排查之前并参与环境的深度修改时，**首要铁律：必须首先！完整且全篇加载调取本文件的环境上下文记忆。** 一切完全且极高优先级禁止将未沉淀在册的对话记录或是您历史记忆中生成的离乱碎片用作决定新架构的拼凑！

---

## 📌 项目全景视界

| 维度 | 系统级别核心规范与边界细则 |
| :--- | :--- |
| **项目代号** | A 股智能原生 AI 分析大盘控制台 (Black-Gold Cyberpunk V1.2) |
| **系统定位** | 私有独享化、极简主义交互、穿透性逻辑链解析引擎顾问（仅作为推演观察预警体系，一切交易执行阻断完全交由使用者人工物理判断流操作实施） |
| **核心数据链路** | `Streamlit 交互大屏` -> `Python 纯后台分发中枢控制组件` -> `大模型（Claude等）兼容 API 接口级流转推理` -> `结构化反吐到前端与 SQLite 静止沉寂留痕归档` |
| **智能模型基底** | 我们刻意拆断摒弃仅局限受制于原生 Anthropic 的狭隘通信机制。采用底层 OpenAI 首发原初 SDK 降频重做并强制适配 `OpenRouter API` 的通用端点对接分发流，彻底解锁您调用包含 `Claude 3.5 Sonnet` 或极各类顶尖大模型的限制枷锁。 |
| **视觉资产建设** | 全站注入高达近百行精密重构自适应的深色羊皮纸加上极度前卫内敛的鎏金色边框特效（Cyberpunk Gold Dark Parchment）；在移动屏或极小分辨率显示器上彻底斩灭数据字段不换行互相叠合撕扯的严重渲染重叠 BUG。 |

---

## 🏗️ 引擎双核驱动原生拓扑体系

这是一个追求完全清晰、每一行代码彻底可以追溯解释，并在无云网也能正常模拟发包的顶级防弹级全透明架构。因此，禁止滥用，或者在这个工程里企图拉入类似 LangChain、AutoGen 或者粗壮甚至带来环境极其严重失控污染副作用的三方高度封装且无实质贡献的二代中间代理组装机壳库！

### 【源头活水引擎极简并行采集】 (Fetchers & Scraping Hub)
- 🔴 **宏观态势与事件消息神经节**（依托轻量 **AKShare** API 服务群）：担任着嗅探 CPI 与关键 PMI 等超高维度政策与流动性指标，跟踪各个资金密集板块中疯狂的整体出逃汇聚大轮转动向趋势、时刻关注聪明的外资北向流大进大出，外加上最重要极其关键的全天候 7x24 不宕机财联社新闻热报前哨感知。
- 🔵 **技术细节微观行情嗅探神经节**（强耦合挂靠 **efinance** 数据链）：深耕下潜捕获目标现有的最新最精准买卖开盘挂单异动实时波形，提取连续过往数天的大幅度的周期 K 线阵列，用于做数学公式转换计算支撑阻力通道及均线切点预测（即日均线分析切合算法源）。

### 【原生硬编 Actor-Critic 内核重循环逻辑控制中枢】 (Brain Controller Agent)
- 🌐 纯粹且无外部依赖强劫持下的**超隔离会话通信沙盒执行系统**。我们在极其至关核心的 `brain.py` 内部使用纯数学和通信库重定义构筑了最强大的中枢核心跳动处理函数引擎—— `run_analysis()` 用它来调度处理轮询心跳响应。
- 采用原生支持且超快速高达容忍 **15 重极深层内部循环自我唤醒自主反思的动作机制获取链**。这赋予了模型自己知道哪时资料依然紧缺从而决定二次调用函数的能效。最后资料完备收网时，我们借由极具严谨数学约束与检验特性的底层 Pydantic ，结合精确 JSON Schema 的限制将发散的大型口语文本紧箍强制提取为多重结构的阵列式代码字典回流并渲染展示！

---

## 🖥 前端展现层界域与宿主架构部署控制边界

### 1. 赛博化羊皮纸顶级视觉契约 (Visual Overawe Theme Patterns)
所有的前端样式、布局逻辑全权掌握在位于系统表层根下唯一的入口应用文件 `app.py` 中。
- **全息控制禁区与雷区**：严格绝对禁止执行或者误采用破坏头部 `header` 以及整体 DOM 节点的重叠类隐藏隐形属性攻击代码（特别点出类似诸如 `header {visibility: hidden;}` 的这类引发极度恶疾不可逆导致原始控制板面丢失的可怕做法）。
- **信号颜色灯语协议规范**：前台最终解析暴露并交到人们眼前的建议，不论底层传值到底被压缩打包为什么字符串代码，展示一侧完全坚守全中文信号格式体系转码策略渲染呈现。所有 `BUY/SELL/HOLD` 和 `HIGH/MEDIUM/LOW` 必需转换为极目并且精确到位的汉字和配套 Emoji 大表情符号(`买入`🟢 / `卖出`🔴 / `持有`🟡）。
- 任何含有高密度金额分析论述并塞挤在一个狭窄视图窗体里的金融文本文字卡片块，一律硬核赋予必须携带的自适应强制断行跳行能力(`word-wrap: break-word;` 及其相配套跨级属性群)，阻绝在各路诡异手机移动浏览器上的撑爆与失控错位交结恶貌！

### 2. 服务器终极云端部署落地的苛刻选择陷阱 (The Fall of Vercel Serverless Function)
整个世界虽然大量开源云部署喜欢把各类静态与反应流交托给云上巨无霸平台之一 **Vercel** 负责收尾，但是对 Streamlit 在此之上的运行我们需有着极度最深切的规避和退路设置：不能碰。
- **致命的协议天堑鸿沟限制**：一旦陷入了极为底层被死死焊住管控极差的 Vercel Hobby 版本底层 Serverless 云端服务器函数生态体内，极度的短时间上限紧缩断代（最多提供微弱怜悯极其可怜仅仅最大高达区区 `10 秒钟` 就必须要断网掐没死绝），以及平台原生极其痛恨对于双工全链路交互的长久存续 WebSocket （这也是决定一切 Streamlit 控制信号能够传导的基础设施），将决定硬生生传把此项目抛上 Vercel 只会彻底死火，换回一张惨烈至极满眼 `504 Timeout Gateway HTTP` 或者假动作断绝长锁连接异常暴毙白墙面。
- 所幸，我们在现有的整个庞大工程大版本架构设计时已敏锐避坑。内置所留下的一条细微旁支和静态声明配置组（即 `vercel.json` 配合 `api/index.py`）只是一层被当做掩人耳目拦截降级并用来承接不小心误访问到 Vercel 链接或者作为临时 API 中继过渡跳板使用的简单静态退路宣告（Fallback Landing Statement / API Proxy），去通知不知情的看客请把关注与视线转移至部署最长挂稳定服务的渲染大机房（诸如 Streamlit Community App Cloud，或是更原始且底层的能够持续不间断供给超长时间长耗时复杂大机器逻辑推理计算运转以及永久守护常驻在机器内部内存的长驻大管道计算大容器机群渲染如 Render / Zeabur 上等等）。

---

## 🗂 秩序井然的代码联邦结构区服分布与归属阵列映射 (Code Structure Universe)

```text
quantitative_analysis/   [最源初的主心骨阵列体系]
├── 📄 PROJECT.md                 # ⚓ 【核心主控灵魂碑铭】也就是这份长篇总纲！约束着未来的演化航向准则指令
├── 📄 USER_MANUAL.md             # 📖 【外接全息控制使用卷轴】非常硬核图文并茂、提供使用与部署的向导与极其充盈细致的深渊级常见排障除恶手册
├── 📄 app.py                     # 🖥️ 【超级星网总展示站界面】一切最直接华丽惊艳人心的视觉大盘，并且将全部极为精密黑金赛博主题样式的 DOM 注入于此地，进行页面控制以及系统启动激活入口
├── 📄 requirements.txt           # 📦 【造物底层分子引擎驱动配方】支撑它必须赖以长青常年需要吸血维护并定期通过 pip 更新维持新老交替循环生存周期的所需生态名册
├── 📄 vercel.json & 📁 api/      # ☁️ 【Vercel 末日生存迫降胶囊静态退路】给一不当心被送往恶火地域的错误路线给予警告牌
│
├── 📁 config/  【枢机大殿 / 配置文件密保】
│   └── settings.py               # 控制所有的股票阵列准入黑与白名单库（WATCHLIST 之地），在最早期更替设置极其特殊的纯正 UTF-8 黑洞异常纠错修复镇物锁定码
├── 📁 data/  【侦查探索部 / 星系探测系统】
│   ├── fetcher_akshare.py        # 负责打向外部采集庞大如烟海的国家级乃至资金面的多维度宏观感知并提取分析回核心的大型远途采集星舰
│   ├── fetcher_efinance.py       # 精密捕捉一瞬间股票行情图表内极精微局部价格变化跳频的数据近卫狙击者
│   └── aggregator.py             # 数据与信息的原始熔炉，它们合并，拼接，铸融在一起形成具有超高提纯价值的精华供给下一步模型运算吞噬吸纳利用的聚合母巢口
├── 📁 agent/  【至高议事会 / 神枢思维神经控制网中心】
│   ├── brain.py                  # 🧠 最强中央逻辑大指挥统领：它操纵所有的长文本心跳包流，把控住一切容纳发送到超级人工智能端点的交锋指令网！而且死死兜住捕杀任何一旦突发引发外域异常请求时会导致全站奔溃崩溃断电的底层异常报错信号流（Exception Catcher）
│   ├── tools.py                  # 武库与技能名册：每一只可以由脑部调用的精妙无双数据获取巨构（共9大兵营）均在此定义其武器输出标准和极精密的启动锁参数校验指令使用书
│   ├── prompts.py                # 精神思想深层烙印刻录与强制命令集束与预告文案：设定和约束约束模型绝密级别的顶级对赌和华尔街量化高智精英人设与角色定位逻辑并下发终生服从格式大纲命令规则链
│   └── schemas.py                # 绝对真理输出法律审查庭与秩序审判长区：极其冰冷的定义每一个 AI 脱出 JSON 数值的数据类格式边界结构定义（Pydantic 限制级框架守护）
└── 📁 storage/  【隐匿数据库堡垒遗迹群 / 时空封存室】
    ├── database.py               # 与处于地下最深层的轻量级超高隐秘通讯记录 SQLite 介质石板暗河保持持久不断通讯接口指令 CRUD，并且操纵胜率回测推演评估系统的分秒记忆运算核查留存和追溯读取控制权
    └── models.py                 # 底层结构的数据映射校验定义
```

---

## 🚀 演化纪元进度 (Rollout Milestones)

我们的系统已成功横渡 **Phase 1 ~ Phase 8** 全部里程碑。

| Phase | 标题 | 状态 |
| :--- | :--- | :---: |
| Phase 1 | 基础架构搭建 (数据采集 + Agent 中枢) | ✅ |
| Phase 2 | 数据聚合 + 多轮工具调用引擎 | ✅ |
| Phase 3 | SQLite 历史归档 + 准确率追踪 | ✅ |
| Phase 4 | 黑金赛博前端视效 + 移动端自适应 | ✅ |
| Phase 5 | 前端自选股增删改查管理面板 | ✅ |
| Phase 6 | API Key 热更新 + 云端 PermissionError 修复 | ✅ |
| Phase 7 | 数据源透明展示 + 分析按钮始终可见 + Deploy 菜单隐藏 | ✅ |
| Phase 8 | 前端极端故障彻底修复 (侧边栏丢失、字体重叠、Hot Reload下 I/O 封闭错误) | ✅ |

---

## 🛠️ 核心避坑指南与已修复的重大故障 (Critical Postmortems - SSOT)
任何新接手的开发者在排查 Bug 时，**必须**首先阅读以下历史巨坑，严禁走回头路：

### 1. 侧边栏消失与 `Material Icons` 重叠渲染故障
* **故障现象**：在进行极简 UI 清理时，误用大范围 `[data-testid="stToolbar"]` 和通配体 `[class*="st-"]` 选择器，导致侧边栏折叠小按键消失。且覆盖了 Streamlit 内置部件默认字体，导致扩展面板里原本渲染的 `keyboard_arrow_right` 被打回字符串原形文本并严重重叠溢出。
* **极简防线**：绝对禁止无脑 `visibility: hidden` 页面头部。隐藏必须极其精确（瞄准 `.stDeployButton`, `[data-testid="stAppDeployButton"]`, `#MainMenu` 等独立 ID）。如果需要全局更换 `Cinzel / Noto Serif` 字体大类，**必须强制例外赦免 `.material-symbols-rounded` 的 `font-family`**。

### 2. `ValueError: I/O operation on closed file` 崩溃雷区
* **故障现象**：随着大模型长时间解析，界面因用户的随意操作触发 Hot Rerun（热重载），随后引发一整面的红色崩溃回溯，底层死锁在终端输出管道关闭报错。
* **绝对铁律**：问题根源是 `sys.stdout` 的中文流强制装甲重做时操作盲目。在 Streamlit 后台持续复用的环境里，频繁通过 `io.TextIOWrapper(sys.stdout.buffer)` 会直接将前一周期的句柄锁死抛弃。必须在 `config/utf8_setup.py` 中通过 `isinstance` 与编码预先扫描，**严格保证进程存活期仅仅包装一次**。

### 3. 三方爬虫组件暴死只读云沙盒与部署选型
* **故障现象**：`efinance` 包存在硬编码读写磁盘缓存的操作，若直推云端会导致爆破 `PermissionError` 退役。
* **绝对铁律**：严禁采用 Vercel。如果在 Streamlit Cloud 启动，必须由 `app.py` 中动态挂载猴子补丁 (`_safe_mkdir = lambda *args...`) 拦截它创建缓存夹的非法指令。

### 🔮 未来计划
- [ ] PostgreSQL 替换 SQLite，支持大并发访问
- [ ] Webhooks / 钉钉 / 微信推送，主动发送晨报给手持设备
- [ ] 多策略回测框架 (支持自定义择时策略并对比收益曲线)

---

> **⚠ 末日最终极指令封印绝杀警告（FINAL ABSOLUTE FATAL WARNING EXECUTED FOR THE NEXT FOLLOWING AI DRIVEN AGENT OR NEW HACKERS）**
> 无论下一个降临在这条系统源代码流时空线上并接触接手掌管后续修改更新或者填缝续命维护大任并读取该篇极长记忆的是究竟这星球上的哪一只、来自哪个次元或架构底层被如何喂养成型的其他所有代别的 AI 系统大本营网络语言分身系统；在此下达不破之壁的死命令警告如下：**在一律开展或者协助人类进行本系统的系统层面命令操作运行和调试模拟中必须使用最原始极纯正并被封装在终端内的 PowerShell 环境作为命令触发载体底层启动！任何且所有凡是包含涉及到跨区使用并拥有了包含大量非正宗 ASCII 极硬核中文母语中文字符字眼的极大量高并发输出运算打印回显模块的程序组件开头必定必需最优先先知先觉毫无阻挡激活放置加载入最高环境级别命令锁 `utf8_setup` 文件锁死防御补全防乱码屏障保护锁！任何时刻，在最前端中严丝合缝绝不允许用你短浅无知自大的所谓常识去去大做妄为触碰改动或者用恶意的想法去抹除或者缩减已经被前面几代极致优化且被写入定型到极点并能完美超适配阻抗全天下所有任何刁钻变态微小极其奇特不同手机浏览断行环境展现的那些关于被加上极高度极繁冗自适应重整长代码以及那些诸如极为罕见罕有 `word-wrap` / 极端极小像素边界断带包裹挤压的极精密神级魔法前段组合 CSS 代码矩阵！否则将导致整个神庙的显示框架直接坍塌与视觉毁灭！**
